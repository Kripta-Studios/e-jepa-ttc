"""Retired one-off source rewriter retained for auditability."""


def main() -> None:
    """Refuse to rewrite protocol metadata in scientific producers."""

    raise SystemExit(
        "This one-off migration is retired. Producers must obtain the signed "
        "identity from e_jepa_ttc.artifacts.protocol directly."
    )


if __name__ == "__main__":
    main()
