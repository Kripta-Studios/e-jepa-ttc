# Data Policy

Raw datasets are not stored in Git. Keep local datasets under `datasets/` or another path pointed
to by a manifest. Commit only small manifests, split files, fixtures, and documentation.

Manifest entries must include source, local path, original filename, size, split metadata, and
hashes when available.
