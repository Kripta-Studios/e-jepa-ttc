# Contributing

Keep changes scoped to the milestone being implemented. Do not add benchmark claims unless they
are regenerated from committed code and stored metrics. Run:

```bash
make test
make lint
```

Large datasets, checkpoints, predictions, and videos must remain outside Git.
